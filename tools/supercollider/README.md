# SuperCollider Setup

This folder contains scripts and patches for SuperCollider.

To run `init.scd`:
1. Open in SCIDE and evaluate the file.
2. Or, run via command line with: `sclang sc/init.scd`

These scripts are designed to be modular, generative sound sources for the `soundscapes` project.